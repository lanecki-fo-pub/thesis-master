\documentclass{article}
%% # packages %%

%% # markdown %%
\usepackage{caption,booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\hypersetup{hidelinks = true}

% conversion breaks without this
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

%% # document %%
\begin{document}
$body$
\end{document}
